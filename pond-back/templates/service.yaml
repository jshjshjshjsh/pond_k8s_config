apiVersion: v1
kind: Service
metadata:
  name: {{ include "pond-back.fullname" . }}
  labels:
    {{- include "pond-back.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pond-back.selectorLabels" . | nindent 4 }}
