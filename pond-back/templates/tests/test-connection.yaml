apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pond-back.fullname" . }}-test-connection"
  labels:
    {{- include "pond-back.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pond-back.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
