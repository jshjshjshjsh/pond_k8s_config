apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pond-front.fullname" . }}-test-connection"
  labels:
    {{- include "pond-front.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pond-front.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
